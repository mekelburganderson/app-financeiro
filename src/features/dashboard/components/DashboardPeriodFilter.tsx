import type { DashboardPreset } from '../types'
export function DashboardPeriodFilter({preset,start,end,valid,onPreset,onStart,onEnd}:{preset:DashboardPreset;start:string;end:string;valid:boolean;onPreset:(value:DashboardPreset)=>void;onStart:(value:string)=>void;onEnd:(value:string)=>void}){
  return <div className="dashboard-period" role="group" aria-label="Período do dashboard"><label htmlFor="dashboard-period">Período<select id="dashboard-period" value={preset} onChange={(event)=>onPreset(event.target.value as DashboardPreset)}><option value="this_month">Este mês</option><option value="last_month">Mês passado</option><option value="last_3_months">Últimos 3 meses</option><option value="last_6_months">Últimos 6 meses</option><option value="this_year">Este ano</option><option value="custom">Personalizado</option></select></label>{preset==='custom'&&<><label htmlFor="dashboard-start">Data inicial<input id="dashboard-start" type="date" value={start} onChange={(event)=>onStart(event.target.value)}/></label><label htmlFor="dashboard-end">Data final<input id="dashboard-end" type="date" value={end} onChange={(event)=>onEnd(event.target.value)}/></label>{!valid&&<p role="alert">Informe um período válido.</p>}</>}</div>
}
