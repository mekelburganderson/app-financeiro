# Copie para .env.local e configure a URL e a chave pública do projeto.
# Todas as variáveis VITE_* ficam acessíveis no navegador.
# Nunca use uma chave secret ou service_role no frontend.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
